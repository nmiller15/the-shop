const authenticateUser = (req, res, next) => {
  const sessionId = req.cookie.session_id;

  // query the database to find a session

  // query the database for the user information that matches the session id
};
