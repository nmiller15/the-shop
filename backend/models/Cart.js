class Cart {
  constructor(username) {
    this.username = username;
    this.id = null;
    this.totalCost = 0;
    this.contents = [];
  }

  // addToCart(product) {}

  // removeFromCart(product) {}
}
